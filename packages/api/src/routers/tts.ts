import { router } from "../index";

// Free TTS implementation removed - using browser TTS (Web Speech API) instead
// Browser TTS is free, doesn't require API keys, and works offline
// For better quality voices, users can select from their system's available voices

export const ttsRouter = router({});
